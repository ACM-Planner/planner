// Initial state
const INITIAL_STATE = {
  id: null,
};

// Reducer
export default function reducer(state = INITIAL_STATE, action = {}) {
  switch (action.type) {
    // do reducer stuff
    default: return state;
  }
}
